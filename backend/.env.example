# ============================================================================
# Backend Environment Configuration
# ============================================================================
# Copy this file to .env and customize for your environment

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
NOC_DATABASE=localhost
NOC_USERNAME=postgres
NOC_PASSWORD=postgres
NOC_DATABASE_PORT=5432
NOC_DATABASE_NAME=noc
NOC_DATABASE_SSL=false

# ============================================================================
# JWT CONFIGURATION
# ============================================================================
# Secret key for JWT token generation (change in production!)
SECRET_KEY=your-secret-key-change-this-in-production

# JWT algorithm
ALGORITHM=HS256

# JWT token expiration time (minutes)
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ============================================================================
# DEFAULT ADMIN CREDENTIALS
# ============================================================================
# Credentials for initial admin user (created on first startup)
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=admin

# ============================================================================
# OIDC BACKEND AUTHENTICATION (NEW)
# ============================================================================
# OIDC provider for backend-to-NiFi authentication
# If set, the backend will use this OIDC provider from oidc_providers.yaml
# to authenticate with NiFi instances instead of certificates or username/password.
#
# Leave empty to use traditional authentication (certificates/username-password)
# Set to provider ID from oidc_providers.yaml to enable OIDC backend authentication
#
# Example: OIDC_BACKEND_PROVIDER=nifi_backend
# See: OIDC_BACKEND_AUTHENTICATION.md for setup guide
OIDC_BACKEND_PROVIDER=

# ============================================================================
# NOTES
# ============================================================================
# 1. Database Configuration:
#    - Ensure PostgreSQL is running and accessible
#    - Database will be created automatically if it doesn't exist
#
# 2. JWT Configuration:
#    - Generate a strong secret key for production:
#      python -c "import secrets; print(secrets.token_urlsafe(32))"
#
# 3. OIDC Backend Authentication:
#    - Configure OIDC provider in config/oidc_providers.yaml first
#    - Set provider's 'backend: true' to prevent showing on user login page
#    - Set OIDC_BACKEND_PROVIDER to the provider ID
#    - All NiFi instances will use this OIDC provider for authentication
#
# 4. For user login OIDC configuration:
#    - See config/oidc_providers.yaml
#    - See OIDC_SETUP_GUIDE.md
#
# 5. For development:
#    - Use .env file in backend/ directory
#    - For production, use environment variables or secrets management
